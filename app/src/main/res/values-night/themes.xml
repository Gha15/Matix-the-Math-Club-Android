<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark counterpart so the native window matches the app's dark mode
         and there is no white flash before the boot screen paints. -->
    <style name="Theme.Matix" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/bg</item>
        <item name="android:colorBackground">@color/bg</item>
    </style>
</resources>
